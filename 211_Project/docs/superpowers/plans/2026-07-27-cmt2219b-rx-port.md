# CMT2219B 接收端驱动移植 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `d:/_Project/GD32_Control/Project` 的 GD32E230 工程中新增 PH2219BBA/CMT2219B 接收端驱动，按官方 CMT2300A Demo 参数接收 433.920 MHz、GFSK、9.6 kbps、32 字节固定包。

**Architecture:** 新增 `Hardware/cmt2219b/`，接收端独立分为端口层、3 线 SPI 层、参数表、芯片控制层、应用接收封装层。现有 `Hardware/cmt2119b/` 发射端保持不变，主程序通过 `radio_tx` 和 `radio_rx` 同时使用收发功能。

**Tech Stack:** GD32E230 标准外设库、C99、Keil uVision ARMCLANG、GPIO 模拟 3 线 SPI、CMT2300A 官方 Demo 参数。

## Global Constraints

- MCU：GD32E230。
- 接收模块：PH2219BBA/CMT2219B。
- 接收端引脚：FCSB=PB12，CSB=PB13，SDIO=PB15，CLK=PB14，GPIO3=PB10。
- 发送端 PH2119BBA/CMT2119B 引脚和驱动保持不变：FCSB=PB0，CSB=PA4，SDIO=PA7，SCLK=PA5，GPIO3=PB1。
- 接收端先按官方 CMT2300A Demo 参数移植：433.920 MHz、GFSK、9.6 kbps、固定包长 32 字节、白化关闭、CRC 关闭。
- 不使用 GD32E230 硬件 SPI，使用 GPIO 模拟 3 线 SPI。
- 不把收发驱动合并成一个大 `radio` 驱动。
- 未经用户明确授权，不执行 `git commit` 或 `git push`；任务中的提交步骤只在获得授权后执行。

---

## File Structure

### Create

- `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_port.h`  
  接收模块引脚定义、GPIO 操作接口、延时接口。

- `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_port.c`  
  GD32E230 PB12/PB13/PB14/PB15/PB10 初始化和电平操作。

- `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_spi.h`  
  CMT2219B 3 线 SPI 接口声明。

- `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_spi.c`  
  CMT2219B 寄存器读写、FIFO 读写时序。

- `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_params.h`  
  从官方 Demo 移植来的无线参数表。

- `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b.h`  
  芯片控制层公开接口。

- `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b.c`  
  参数写入、状态切换、FIFO 控制、中断配置、接收状态维护。

- `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/radio_rx.h`  
  应用层接收接口声明。

- `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/radio_rx.c`  
  应用层接收初始化和轮询收包接口。

### Modify

- `d:/_Project/GD32_Control/Project/Project/Project.uvprojx`  
  新增 `..\Hardware\cmt2219b` 头文件路径，并把新增 `.c/.h` 加入 Keil 工程。

- `d:/_Project/GD32_Control/Project/User/main.c`  
  加入 `radio_rx` 首版验证逻辑，收到数据后用调试变量观察。

---

## Task 1: 创建 CMT2219B 端口层

**Files:**
- Create: `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_port.h`
- Create: `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_port.c`

**Interfaces:**
- Consumes: `gd32e23x.h` GPIO/RCU API，`systick.h` 的 `delay_us()` 和 `delay_ms()`。
- Produces:
  - `void cmt2219b_port_init(void);`
  - `void cmt2219b_csb_high(void);`
  - `void cmt2219b_csb_low(void);`
  - `void cmt2219b_fcsb_high(void);`
  - `void cmt2219b_fcsb_low(void);`
  - `void cmt2219b_clk_high(void);`
  - `void cmt2219b_clk_low(void);`
  - `void cmt2219b_sdio_high(void);`
  - `void cmt2219b_sdio_low(void);`
  - `uint8_t cmt2219b_sdio_read(void);`
  - `void cmt2219b_sdio_output(void);`
  - `void cmt2219b_sdio_input(void);`
  - `uint8_t cmt2219b_gpio3_read(void);`
  - `void cmt2219b_delay_us(uint32_t us);`
  - `void cmt2219b_delay_ms(uint32_t ms);`

- [ ] **Step 1: 新建端口层头文件**

Create `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_port.h`:

```c
#ifndef __CMT2219B_PORT_H
#define __CMT2219B_PORT_H

#include "gd32e23x.h"
#include <stdint.h>

/*
 * PH2219BBA / CMT2219B module connection:
 *
 * FCSB  -> PB12
 * CSB   -> PB13
 * SDIO  -> PB15
 * CLK   -> PB14
 * GPIO3 -> PB10
 * VCC   -> 3V3
 * GND   -> GND
 * ANT   -> 433MHz receive antenna
 */

#define CMT2219B_FCSB_PORT      GPIOB
#define CMT2219B_FCSB_PIN       GPIO_PIN_12
#define CMT2219B_FCSB_RCU       RCU_GPIOB

#define CMT2219B_CSB_PORT       GPIOB
#define CMT2219B_CSB_PIN        GPIO_PIN_13
#define CMT2219B_CSB_RCU        RCU_GPIOB

#define CMT2219B_CLK_PORT       GPIOB
#define CMT2219B_CLK_PIN        GPIO_PIN_14
#define CMT2219B_CLK_RCU        RCU_GPIOB

#define CMT2219B_SDIO_PORT      GPIOB
#define CMT2219B_SDIO_PIN       GPIO_PIN_15
#define CMT2219B_SDIO_RCU       RCU_GPIOB

#define CMT2219B_GPIO3_PORT     GPIOB
#define CMT2219B_GPIO3_PIN      GPIO_PIN_10
#define CMT2219B_GPIO3_RCU      RCU_GPIOB

void cmt2219b_port_init(void);

void cmt2219b_csb_high(void);
void cmt2219b_csb_low(void);

void cmt2219b_fcsb_high(void);
void cmt2219b_fcsb_low(void);

void cmt2219b_clk_high(void);
void cmt2219b_clk_low(void);

void cmt2219b_sdio_high(void);
void cmt2219b_sdio_low(void);
uint8_t cmt2219b_sdio_read(void);

void cmt2219b_sdio_output(void);
void cmt2219b_sdio_input(void);

uint8_t cmt2219b_gpio3_read(void);

void cmt2219b_delay_us(uint32_t us);
void cmt2219b_delay_ms(uint32_t ms);

#endif
```

- [ ] **Step 2: 新建端口层源码文件**

Create `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_port.c`:

```c
#include "cmt2219b_port.h"
#include "systick.h"

void cmt2219b_port_init(void)
{
    rcu_periph_clock_enable(CMT2219B_FCSB_RCU);
    rcu_periph_clock_enable(CMT2219B_CSB_RCU);
    rcu_periph_clock_enable(CMT2219B_CLK_RCU);
    rcu_periph_clock_enable(CMT2219B_SDIO_RCU);
    rcu_periph_clock_enable(CMT2219B_GPIO3_RCU);

    gpio_mode_set(CMT2219B_FCSB_PORT, GPIO_MODE_OUTPUT, GPIO_PUPD_NONE, CMT2219B_FCSB_PIN);
    gpio_output_options_set(CMT2219B_FCSB_PORT, GPIO_OTYPE_PP, GPIO_OSPEED_50MHZ, CMT2219B_FCSB_PIN);

    gpio_mode_set(CMT2219B_CSB_PORT, GPIO_MODE_OUTPUT, GPIO_PUPD_NONE, CMT2219B_CSB_PIN);
    gpio_output_options_set(CMT2219B_CSB_PORT, GPIO_OTYPE_PP, GPIO_OSPEED_50MHZ, CMT2219B_CSB_PIN);

    gpio_mode_set(CMT2219B_CLK_PORT, GPIO_MODE_OUTPUT, GPIO_PUPD_NONE, CMT2219B_CLK_PIN);
    gpio_output_options_set(CMT2219B_CLK_PORT, GPIO_OTYPE_PP, GPIO_OSPEED_50MHZ, CMT2219B_CLK_PIN);

    cmt2219b_sdio_output();

    gpio_mode_set(CMT2219B_GPIO3_PORT, GPIO_MODE_INPUT, GPIO_PUPD_NONE, CMT2219B_GPIO3_PIN);

    cmt2219b_csb_high();
    cmt2219b_fcsb_high();
    cmt2219b_clk_low();
    cmt2219b_sdio_high();
}

void cmt2219b_csb_high(void)
{
    gpio_bit_set(CMT2219B_CSB_PORT, CMT2219B_CSB_PIN);
}

void cmt2219b_csb_low(void)
{
    gpio_bit_reset(CMT2219B_CSB_PORT, CMT2219B_CSB_PIN);
}

void cmt2219b_fcsb_high(void)
{
    gpio_bit_set(CMT2219B_FCSB_PORT, CMT2219B_FCSB_PIN);
}

void cmt2219b_fcsb_low(void)
{
    gpio_bit_reset(CMT2219B_FCSB_PORT, CMT2219B_FCSB_PIN);
}

void cmt2219b_clk_high(void)
{
    gpio_bit_set(CMT2219B_CLK_PORT, CMT2219B_CLK_PIN);
}

void cmt2219b_clk_low(void)
{
    gpio_bit_reset(CMT2219B_CLK_PORT, CMT2219B_CLK_PIN);
}

void cmt2219b_sdio_high(void)
{
    gpio_bit_set(CMT2219B_SDIO_PORT, CMT2219B_SDIO_PIN);
}

void cmt2219b_sdio_low(void)
{
    gpio_bit_reset(CMT2219B_SDIO_PORT, CMT2219B_SDIO_PIN);
}

uint8_t cmt2219b_sdio_read(void)
{
    return gpio_input_bit_get(CMT2219B_SDIO_PORT, CMT2219B_SDIO_PIN) ? 1 : 0;
}

void cmt2219b_sdio_output(void)
{
    gpio_mode_set(CMT2219B_SDIO_PORT, GPIO_MODE_OUTPUT, GPIO_PUPD_NONE, CMT2219B_SDIO_PIN);
    gpio_output_options_set(CMT2219B_SDIO_PORT, GPIO_OTYPE_PP, GPIO_OSPEED_50MHZ, CMT2219B_SDIO_PIN);
}

void cmt2219b_sdio_input(void)
{
    gpio_mode_set(CMT2219B_SDIO_PORT, GPIO_MODE_INPUT, GPIO_PUPD_PULLUP, CMT2219B_SDIO_PIN);
}

uint8_t cmt2219b_gpio3_read(void)
{
    return gpio_input_bit_get(CMT2219B_GPIO3_PORT, CMT2219B_GPIO3_PIN) ? 1 : 0;
}

void cmt2219b_delay_us(uint32_t us)
{
    delay_us(us);
}

void cmt2219b_delay_ms(uint32_t ms)
{
    delay_ms(ms);
}
```

- [ ] **Step 3: 编译验证端口层语法**

先暂时不用修改主程序，只把文件保存。完成 Task 5 加入 Keil 工程后再统一编译。

Expected after Task 5 build: no errors from `cmt2219b_port.c`.

- [ ] **Step 4: 授权后提交本任务**

如果用户明确授权执行提交：

```bash
git add Hardware/cmt2219b/cmt2219b_port.h Hardware/cmt2219b/cmt2219b_port.c
git commit -m "feat: add cmt2219b port layer"
```

如果用户没有授权提交，跳过本步骤。

---

## Task 2: 创建 CMT2219B 3 线 SPI 层

**Files:**
- Create: `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_spi.h`
- Create: `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_spi.c`

**Interfaces:**
- Consumes: Task 1 的 `cmt2219b_port` 接口。
- Produces:
  - `void cmt2219b_spi_init(void);`
  - `void cmt2219b_spi_write_reg(uint8_t addr, uint8_t dat);`
  - `uint8_t cmt2219b_spi_read_reg(uint8_t addr);`
  - `void cmt2219b_spi_write_fifo(const uint8_t *buf, uint16_t len);`
  - `void cmt2219b_spi_read_fifo(uint8_t *buf, uint16_t len);`

- [ ] **Step 1: 新建 SPI 头文件**

Create `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_spi.h`:

```c
#ifndef __CMT2219B_SPI_H
#define __CMT2219B_SPI_H

#include <stdint.h>

void cmt2219b_spi_init(void);

void cmt2219b_spi_write_reg(uint8_t addr, uint8_t dat);
uint8_t cmt2219b_spi_read_reg(uint8_t addr);

void cmt2219b_spi_write_fifo(const uint8_t *buf, uint16_t len);
void cmt2219b_spi_read_fifo(uint8_t *buf, uint16_t len);

#endif
```

- [ ] **Step 2: 新建 SPI 源码文件**

Create `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_spi.c`:

```c
#include "cmt2219b_spi.h"
#include "cmt2219b_port.h"

static void cmt2219b_spi_delay(void)
{
    volatile uint32_t n = 20;
    while (n--) {
    }
}

static void cmt2219b_spi_delay_us(void)
{
    cmt2219b_delay_us(1);
}

void cmt2219b_spi_init(void)
{
    cmt2219b_port_init();

    cmt2219b_csb_high();
    cmt2219b_fcsb_high();
    cmt2219b_clk_low();

    cmt2219b_sdio_output();
    cmt2219b_sdio_high();

    cmt2219b_spi_delay();
}

static void cmt2219b_spi_send(uint8_t data)
{
    uint8_t i;

    for (i = 0; i < 8; i++) {
        cmt2219b_clk_low();

        if (data & 0x80) {
            cmt2219b_sdio_high();
        } else {
            cmt2219b_sdio_low();
        }

        cmt2219b_spi_delay();

        data <<= 1;

        cmt2219b_clk_high();
        cmt2219b_spi_delay();
    }
}

static uint8_t cmt2219b_spi_recv(void)
{
    uint8_t i;
    uint8_t data = 0;

    for (i = 0; i < 8; i++) {
        cmt2219b_clk_low();
        cmt2219b_spi_delay();

        data <<= 1;

        cmt2219b_clk_high();

        if (cmt2219b_sdio_read()) {
            data |= 0x01;
        }

        cmt2219b_spi_delay();
    }

    return data;
}

void cmt2219b_spi_write_reg(uint8_t addr, uint8_t dat)
{
    cmt2219b_sdio_output();
    cmt2219b_sdio_high();

    cmt2219b_clk_low();

    cmt2219b_fcsb_high();
    cmt2219b_csb_low();

    cmt2219b_spi_delay();
    cmt2219b_spi_delay();

    cmt2219b_spi_send(addr & 0x7F);
    cmt2219b_spi_send(dat);

    cmt2219b_clk_low();

    cmt2219b_spi_delay();
    cmt2219b_spi_delay();

    cmt2219b_csb_high();

    cmt2219b_sdio_high();
    cmt2219b_sdio_input();

    cmt2219b_fcsb_high();
}

uint8_t cmt2219b_spi_read_reg(uint8_t addr)
{
    uint8_t dat;

    cmt2219b_sdio_output();
    cmt2219b_sdio_high();

    cmt2219b_clk_low();

    cmt2219b_fcsb_high();
    cmt2219b_csb_low();

    cmt2219b_spi_delay();
    cmt2219b_spi_delay();

    cmt2219b_spi_send(addr | 0x80);

    cmt2219b_sdio_input();

    dat = cmt2219b_spi_recv();

    cmt2219b_clk_low();

    cmt2219b_spi_delay();
    cmt2219b_spi_delay();

    cmt2219b_csb_high();

    cmt2219b_sdio_input();
    cmt2219b_fcsb_high();

    return dat;
}

void cmt2219b_spi_write_fifo(const uint8_t *buf, uint16_t len)
{
    uint16_t i;

    cmt2219b_fcsb_high();
    cmt2219b_csb_high();
    cmt2219b_clk_low();

    cmt2219b_sdio_output();

    for (i = 0; i < len; i++) {
        cmt2219b_fcsb_low();

        cmt2219b_spi_delay();
        cmt2219b_spi_delay();

        cmt2219b_spi_send(buf[i]);

        cmt2219b_clk_low();

        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();

        cmt2219b_fcsb_high();

        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
    }

    cmt2219b_sdio_input();
    cmt2219b_fcsb_high();
}

void cmt2219b_spi_read_fifo(uint8_t *buf, uint16_t len)
{
    uint16_t i;

    cmt2219b_fcsb_high();
    cmt2219b_csb_high();
    cmt2219b_clk_low();

    cmt2219b_sdio_input();

    for (i = 0; i < len; i++) {
        cmt2219b_fcsb_low();

        cmt2219b_spi_delay();
        cmt2219b_spi_delay();

        buf[i] = cmt2219b_spi_recv();

        cmt2219b_clk_low();

        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();

        cmt2219b_fcsb_high();

        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
        cmt2219b_spi_delay_us();
    }

    cmt2219b_sdio_input();
    cmt2219b_fcsb_high();
}
```

- [ ] **Step 3: 硬件波形验证点**

完成 Task 5 加入 Keil 工程并下载后，在 `cmt2219b_spi_write_reg()` 或 `cmt2219b_spi_read_reg()` 处打断点单步运行，用逻辑分析仪观察：

```text
PB13 / CSB  : 寄存器访问期间拉低，结束后拉高
PB14 / CLK  : 每字节 8 个时钟
PB15 / SDIO : 写寄存器时输出地址和数据，读寄存器地址后切换输入
PB12 / FCSB : 寄存器访问保持高，FIFO 访问按字节拉低
```

- [ ] **Step 4: 授权后提交本任务**

如果用户明确授权执行提交：

```bash
git add Hardware/cmt2219b/cmt2219b_spi.h Hardware/cmt2219b/cmt2219b_spi.c
git commit -m "feat: add cmt2219b spi layer"
```

如果用户没有授权提交，跳过本步骤。

---

## Task 3: 创建 CMT2219B 参数表

**Files:**
- Create: `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_params.h`

**Interfaces:**
- Consumes: 官方 `CMT2300A_Demo_Rx/USER/radio/cmt2300a_params.h` 参数。
- Produces:
  - `g_cmt2219b_cmt_bank[]`
  - `g_cmt2219b_system_bank[]`
  - `g_cmt2219b_frequency_bank[]`
  - `g_cmt2219b_data_rate_bank[]`
  - `g_cmt2219b_baseband_bank[]`
  - `g_cmt2219b_tx_bank[]`
  - `CMT2219B_*_BANK_SIZE` 宏。

- [ ] **Step 1: 新建参数文件**

Create `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b_params.h`:

```c
#ifndef __CMT2219B_PARAMS_H
#define __CMT2219B_PARAMS_H

#include <stdint.h>

/*******************************************************
; Mode                      = Advanced
; Part Number               = CMT2300A official demo parameters, used for first CMT2219B port
; Frequency                 = 433.920 MHz
; Xtal Frequency            = 26.0000 MHz
; Demodulation              = GFSK
; AGC                       = On
; Data Rate                 = 9.6 kbps
; Deviation                 = 20.0 kHz
; Packet Type               = Fixed Length
; Payload Length            = 32
; Whitening                 = Disable
; CRC Options               = None
********************************************************/

static const uint8_t g_cmt2219b_cmt_bank[] = {
    0x00,
    0x66,
    0xEC,
    0x1D,
    0xF0,
    0x80,
    0x14,
    0x08,
    0x91,
    0x02,
    0x02,
    0xD0,
};

static const uint8_t g_cmt2219b_system_bank[] = {
    0xAE,
    0xE0,
    0x35,
    0x00,
    0x00,
    0xF4,
    0x10,
    0xE2,
    0x42,
    0x20,
    0x00,
    0x81,
};

static const uint8_t g_cmt2219b_frequency_bank[] = {
    0x42,
    0x71,
    0xCE,
    0x1C,
    0x42,
    0x5B,
    0x1C,
    0x1C,
};

static const uint8_t g_cmt2219b_data_rate_bank[] = {
    0xCA,
    0x60,
    0x10,
    0x33,
    0xE1,
    0x36,
    0x19,
    0x0A,
    0x9F,
    0x38,
    0x29,
    0x29,
    0xC0,
    0x94,
    0x0A,
    0x53,
    0x08,
    0x00,
    0xB4,
    0x00,
    0x00,
    0x01,
    0x00,
    0x00,
};

static const uint8_t g_cmt2219b_baseband_bank[] = {
    0x12,
    0x08,
    0x00,
    0xAA,
    0x04,
    0x00,
    0x00,
    0x00,
    0x00,
    0x00,
    0xD4,
    0x2D,
    0xAA,
    0x00,
    0x1F,
    0x00,
    0x00,
    0x00,
    0x00,
    0x00,
    0x00,
    0x00,
    0x00,
    0x60,
    0xFF,
    0x00,
    0x00,
    0x1F,
    0x10,
};

static const uint8_t g_cmt2219b_tx_bank[] = {
    0x70,
    0x9A,
    0x0C,
    0x00,
    0x0F,
    0x90,
    0x00,
    0x8A,
    0x18,
    0x3F,
    0x7F,
};

#define CMT2219B_CMT_BANK_SIZE          ((uint8_t)sizeof(g_cmt2219b_cmt_bank))
#define CMT2219B_SYSTEM_BANK_SIZE       ((uint8_t)sizeof(g_cmt2219b_system_bank))
#define CMT2219B_FREQUENCY_BANK_SIZE    ((uint8_t)sizeof(g_cmt2219b_frequency_bank))
#define CMT2219B_DATA_RATE_BANK_SIZE    ((uint8_t)sizeof(g_cmt2219b_data_rate_bank))
#define CMT2219B_BASEBAND_BANK_SIZE     ((uint8_t)sizeof(g_cmt2219b_baseband_bank))
#define CMT2219B_TX_BANK_SIZE           ((uint8_t)sizeof(g_cmt2219b_tx_bank))

#endif
```

- [ ] **Step 2: 参数长度检查**

确认数组长度：

```text
CMT Bank       = 12
System Bank    = 12
Frequency Bank = 8
Data Rate Bank = 24
Baseband Bank  = 29
Tx Bank        = 11
```

这些长度要和官方 Demo 一致。

- [ ] **Step 3: 授权后提交本任务**

如果用户明确授权执行提交：

```bash
git add Hardware/cmt2219b/cmt2219b_params.h
git commit -m "feat: add cmt2219b rf parameters"
```

如果用户没有授权提交，跳过本步骤。

---

## Task 4: 创建 CMT2219B 芯片控制层

**Files:**
- Create: `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b.h`
- Create: `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b.c`

**Interfaces:**
- Consumes: Task 2 的 SPI 接口，Task 3 的参数表。
- Produces:
  - `uint8_t cmt2219b_init(void);`
  - `uint8_t cmt2219b_is_exist(void);`
  - `uint8_t cmt2219b_read_reg(uint8_t addr);`
  - `void cmt2219b_write_reg(uint8_t addr, uint8_t dat);`
  - `void cmt2219b_read_fifo(uint8_t *buf, uint8_t len);`
  - `void cmt2219b_write_fifo(const uint8_t *buf, uint8_t len);`
  - `uint8_t cmt2219b_go_sleep(void);`
  - `uint8_t cmt2219b_go_stby(void);`
  - `uint8_t cmt2219b_go_rx(void);`
  - `void cmt2219b_enable_read_fifo(void);`
  - `void cmt2219b_clear_rx_fifo(void);`
  - `void cmt2219b_clear_interrupt_flags(void);`
  - `uint8_t cmt2219b_packet_received(void);`

- [ ] **Step 1: 新建芯片控制层头文件**

Create `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b.h`:

```c
#ifndef __CMT2219B_H
#define __CMT2219B_H

#include <stdint.h>

#define CMT2219B_OK             1
#define CMT2219B_ERROR          0

#define CMT2219B_MAX_FIFO_SIZE  64

uint8_t cmt2219b_init(void);
uint8_t cmt2219b_is_exist(void);

uint8_t cmt2219b_read_reg(uint8_t addr);
void cmt2219b_write_reg(uint8_t addr, uint8_t dat);

void cmt2219b_read_fifo(uint8_t *buf, uint8_t len);
void cmt2219b_write_fifo(const uint8_t *buf, uint8_t len);

uint8_t cmt2219b_go_sleep(void);
uint8_t cmt2219b_go_stby(void);
uint8_t cmt2219b_go_rx(void);

void cmt2219b_enable_read_fifo(void);
void cmt2219b_clear_rx_fifo(void);
void cmt2219b_clear_interrupt_flags(void);

uint8_t cmt2219b_packet_received(void);

#endif
```

- [ ] **Step 2: 新建芯片控制层源码文件**

Create `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/cmt2219b.c`:

```c
#include "cmt2219b.h"
#include "cmt2219b_spi.h"
#include "cmt2219b_port.h"
#include "cmt2219b_params.h"

#define CMT2219B_CMT_BANK_ADDR              0x00
#define CMT2219B_SYSTEM_BANK_ADDR           0x0C
#define CMT2219B_FREQUENCY_BANK_ADDR        0x18
#define CMT2219B_DATA_RATE_BANK_ADDR        0x20
#define CMT2219B_BASEBAND_BANK_ADDR         0x38
#define CMT2219B_TX_BANK_ADDR               0x55

#define CMT2219B_CUS_CMT10                  0x09
#define CMT2219B_CUS_SYS2                   0x0D
#define CMT2219B_CUS_PKT17                  0x48
#define CMT2219B_CUS_MODE_CTL               0x60
#define CMT2219B_CUS_MODE_STA               0x61
#define CMT2219B_CUS_EN_CTL                 0x62
#define CMT2219B_CUS_IO_SEL                 0x65
#define CMT2219B_CUS_INT1_CTL               0x66
#define CMT2219B_CUS_INT2_CTL               0x67
#define CMT2219B_CUS_INT_EN                 0x68
#define CMT2219B_CUS_FIFO_CTL               0x69
#define CMT2219B_CUS_INT_CLR1               0x6A
#define CMT2219B_CUS_INT_CLR2               0x6B
#define CMT2219B_CUS_FIFO_CLR               0x6C
#define CMT2219B_CUS_INT_FLAG               0x6D

#define CMT2219B_GO_STBY                    0x02
#define CMT2219B_GO_RX                      0x08
#define CMT2219B_GO_SLEEP                   0x10

#define CMT2219B_STA_SLEEP                  0x01
#define CMT2219B_STA_STBY                   0x02
#define CMT2219B_STA_RX                     0x05

#define CMT2219B_MASK_CHIP_MODE_STA         0x0F
#define CMT2219B_MASK_CFG_RETAIN            0x10
#define CMT2219B_MASK_RSTN_IN_EN            0x20
#define CMT2219B_MASK_LOCKING_EN            0x20

#define CMT2219B_MASK_LFOSC_RECAL_EN        0x80
#define CMT2219B_MASK_LFOSC_CAL1_EN         0x40
#define CMT2219B_MASK_LFOSC_CAL2_EN         0x20

#define CMT2219B_GPIO3_SEL_INT2             0x20
#define CMT2219B_MASK_INT2_SEL              0x1F
#define CMT2219B_INT_SEL_PKT_DONE           0x19

#define CMT2219B_MASK_PREAM_OK_EN           0x10
#define CMT2219B_MASK_SYNC_OK_EN            0x08
#define CMT2219B_MASK_PKT_DONE_EN           0x01

#define CMT2219B_MASK_SPI_FIFO_RD_WR_SEL    0x01
#define CMT2219B_MASK_FIFO_RX_TX_SEL        0x04
#define CMT2219B_MASK_FIFO_CLR_RX           0x02

#define CMT2219B_MASK_SL_TMO_CLR            0x02
#define CMT2219B_MASK_RX_TMO_CLR            0x01
#define CMT2219B_MASK_TX_DONE_CLR           0x04
#define CMT2219B_MASK_LBD_CLR               0x20
#define CMT2219B_MASK_PREAM_OK_CLR          0x10
#define CMT2219B_MASK_SYNC_OK_CLR           0x08
#define CMT2219B_MASK_NODE_OK_CLR           0x04
#define CMT2219B_MASK_CRC_OK_CLR            0x02
#define CMT2219B_MASK_PKT_DONE_CLR          0x01

static void cmt2219b_config_reg_bank(uint8_t start_addr, const uint8_t *buf, uint8_t len)
{
    uint8_t i;

    for (i = 0; i < len; i++) {
        cmt2219b_write_reg((uint8_t)(start_addr + i), buf[i]);
    }
}

static void cmt2219b_soft_reset(void)
{
    cmt2219b_write_reg(0x7F, 0xFF);
}

static uint8_t cmt2219b_get_chip_status(void)
{
    return cmt2219b_read_reg(CMT2219B_CUS_MODE_STA) & CMT2219B_MASK_CHIP_MODE_STA;
}

static uint8_t cmt2219b_auto_switch_status(uint8_t cmd, uint8_t wait_status)
{
    uint8_t i;

    cmt2219b_write_reg(CMT2219B_CUS_MODE_CTL, cmd);

    for (i = 0; i < 100; i++) {
        cmt2219b_delay_us(100);

        if (cmt2219b_get_chip_status() == wait_status) {
            return CMT2219B_OK;
        }
    }

    return CMT2219B_ERROR;
}

static void cmt2219b_enable_lfosc(uint8_t enable)
{
    uint8_t tmp;

    tmp = cmt2219b_read_reg(CMT2219B_CUS_SYS2);

    if (enable) {
        tmp |= CMT2219B_MASK_LFOSC_RECAL_EN;
        tmp |= CMT2219B_MASK_LFOSC_CAL1_EN;
        tmp |= CMT2219B_MASK_LFOSC_CAL2_EN;
    } else {
        tmp &= (uint8_t)(~CMT2219B_MASK_LFOSC_RECAL_EN);
        tmp &= (uint8_t)(~CMT2219B_MASK_LFOSC_CAL1_EN);
        tmp &= (uint8_t)(~CMT2219B_MASK_LFOSC_CAL2_EN);
    }

    cmt2219b_write_reg(CMT2219B_CUS_SYS2, tmp);
}

static void cmt2219b_config_gpio_interrupt(void)
{
    uint8_t tmp;

    cmt2219b_write_reg(CMT2219B_CUS_IO_SEL, CMT2219B_GPIO3_SEL_INT2);

    tmp = cmt2219b_read_reg(CMT2219B_CUS_INT2_CTL);
    tmp &= (uint8_t)(~CMT2219B_MASK_INT2_SEL);
    tmp |= CMT2219B_INT_SEL_PKT_DONE;
    cmt2219b_write_reg(CMT2219B_CUS_INT2_CTL, tmp);

    cmt2219b_write_reg(CMT2219B_CUS_INT_EN,
                       CMT2219B_MASK_PKT_DONE_EN |
                       CMT2219B_MASK_PREAM_OK_EN |
                       CMT2219B_MASK_SYNC_OK_EN);
}

uint8_t cmt2219b_init(void)
{
    uint8_t tmp;

    cmt2219b_spi_init();

    cmt2219b_soft_reset();
    cmt2219b_delay_ms(20);

    if (cmt2219b_go_stby() != CMT2219B_OK) {
        return CMT2219B_ERROR;
    }

    tmp = cmt2219b_read_reg(CMT2219B_CUS_MODE_STA);
    tmp |= CMT2219B_MASK_CFG_RETAIN;
    tmp &= (uint8_t)(~CMT2219B_MASK_RSTN_IN_EN);
    cmt2219b_write_reg(CMT2219B_CUS_MODE_STA, tmp);

    tmp = cmt2219b_read_reg(CMT2219B_CUS_EN_CTL);
    tmp |= CMT2219B_MASK_LOCKING_EN;
    cmt2219b_write_reg(CMT2219B_CUS_EN_CTL, tmp);

    cmt2219b_enable_lfosc(0);
    cmt2219b_clear_interrupt_flags();

    cmt2219b_config_reg_bank(CMT2219B_CMT_BANK_ADDR,
                             g_cmt2219b_cmt_bank,
                             CMT2219B_CMT_BANK_SIZE);

    cmt2219b_config_reg_bank(CMT2219B_SYSTEM_BANK_ADDR,
                             g_cmt2219b_system_bank,
                             CMT2219B_SYSTEM_BANK_SIZE);

    cmt2219b_config_reg_bank(CMT2219B_FREQUENCY_BANK_ADDR,
                             g_cmt2219b_frequency_bank,
                             CMT2219B_FREQUENCY_BANK_SIZE);

    cmt2219b_config_reg_bank(CMT2219B_DATA_RATE_BANK_ADDR,
                             g_cmt2219b_data_rate_bank,
                             CMT2219B_DATA_RATE_BANK_SIZE);

    cmt2219b_config_reg_bank(CMT2219B_BASEBAND_BANK_ADDR,
                             g_cmt2219b_baseband_bank,
                             CMT2219B_BASEBAND_BANK_SIZE);

    cmt2219b_config_reg_bank(CMT2219B_TX_BANK_ADDR,
                             g_cmt2219b_tx_bank,
                             CMT2219B_TX_BANK_SIZE);

    tmp = cmt2219b_read_reg(CMT2219B_CUS_CMT10);
    tmp &= (uint8_t)(~0x07);
    tmp |= 0x02;
    cmt2219b_write_reg(CMT2219B_CUS_CMT10, tmp);

    cmt2219b_config_gpio_interrupt();
    cmt2219b_enable_lfosc(0);
    cmt2219b_go_sleep();

    return CMT2219B_OK;
}

uint8_t cmt2219b_is_exist(void)
{
    uint8_t back;
    uint8_t dat;

    back = cmt2219b_read_reg(CMT2219B_CUS_PKT17);
    cmt2219b_write_reg(CMT2219B_CUS_PKT17, 0xAA);

    dat = cmt2219b_read_reg(CMT2219B_CUS_PKT17);
    cmt2219b_write_reg(CMT2219B_CUS_PKT17, back);

    return (dat == 0xAA) ? CMT2219B_OK : CMT2219B_ERROR;
}

uint8_t cmt2219b_read_reg(uint8_t addr)
{
    return cmt2219b_spi_read_reg(addr);
}

void cmt2219b_write_reg(uint8_t addr, uint8_t dat)
{
    cmt2219b_spi_write_reg(addr, dat);
}

void cmt2219b_read_fifo(uint8_t *buf, uint8_t len)
{
    if (len > CMT2219B_MAX_FIFO_SIZE) {
        len = CMT2219B_MAX_FIFO_SIZE;
    }

    cmt2219b_spi_read_fifo(buf, len);
}

void cmt2219b_write_fifo(const uint8_t *buf, uint8_t len)
{
    if (len > CMT2219B_MAX_FIFO_SIZE) {
        len = CMT2219B_MAX_FIFO_SIZE;
    }

    cmt2219b_spi_write_fifo(buf, len);
}

uint8_t cmt2219b_go_sleep(void)
{
    return cmt2219b_auto_switch_status(CMT2219B_GO_SLEEP, CMT2219B_STA_SLEEP);
}

uint8_t cmt2219b_go_stby(void)
{
    return cmt2219b_auto_switch_status(CMT2219B_GO_STBY, CMT2219B_STA_STBY);
}

uint8_t cmt2219b_go_rx(void)
{
    return cmt2219b_auto_switch_status(CMT2219B_GO_RX, CMT2219B_STA_RX);
}

void cmt2219b_enable_read_fifo(void)
{
    uint8_t tmp;

    tmp = cmt2219b_read_reg(CMT2219B_CUS_FIFO_CTL);
    tmp &= (uint8_t)(~CMT2219B_MASK_SPI_FIFO_RD_WR_SEL);
    tmp &= (uint8_t)(~CMT2219B_MASK_FIFO_RX_TX_SEL);
    cmt2219b_write_reg(CMT2219B_CUS_FIFO_CTL, tmp);
}

void cmt2219b_clear_rx_fifo(void)
{
    cmt2219b_write_reg(CMT2219B_CUS_FIFO_CLR, CMT2219B_MASK_FIFO_CLR_RX);
}

void cmt2219b_clear_interrupt_flags(void)
{
    cmt2219b_write_reg(CMT2219B_CUS_INT_CLR1,
                       CMT2219B_MASK_TX_DONE_CLR |
                       CMT2219B_MASK_SL_TMO_CLR |
                       CMT2219B_MASK_RX_TMO_CLR);

    cmt2219b_write_reg(CMT2219B_CUS_INT_CLR2,
                       CMT2219B_MASK_LBD_CLR |
                       CMT2219B_MASK_PREAM_OK_CLR |
                       CMT2219B_MASK_SYNC_OK_CLR |
                       CMT2219B_MASK_NODE_OK_CLR |
                       CMT2219B_MASK_CRC_OK_CLR |
                       CMT2219B_MASK_PKT_DONE_CLR);
}

uint8_t cmt2219b_packet_received(void)
{
    return cmt2219b_gpio3_read();
}
```

- [ ] **Step 3: 重点检查寄存器地址**

人工核对下面几个地址和官方 `cmt2300a_defs.h` 一致：

```text
CMT2219B_CUS_MODE_CTL  = 0x60
CMT2219B_CUS_MODE_STA  = 0x61
CMT2219B_CUS_IO_SEL    = 0x65
CMT2219B_CUS_INT2_CTL  = 0x67
CMT2219B_CUS_INT_EN    = 0x68
CMT2219B_CUS_FIFO_CTL  = 0x69
CMT2219B_CUS_FIFO_CLR  = 0x6C
CMT2219B_CUS_INT_FLAG  = 0x6D
```

- [ ] **Step 4: 授权后提交本任务**

如果用户明确授权执行提交：

```bash
git add Hardware/cmt2219b/cmt2219b.h Hardware/cmt2219b/cmt2219b.c
git commit -m "feat: add cmt2219b chip control"
```

如果用户没有授权提交，跳过本步骤。

---

## Task 5: 创建 radio_rx 应用接收封装

**Files:**
- Create: `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/radio_rx.h`
- Create: `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/radio_rx.c`

**Interfaces:**
- Consumes: Task 4 的 `cmt2219b` 接口。
- Produces:
  - `uint8_t radio_rx_init(void);`
  - `uint8_t radio_rx_poll_packet(uint8_t *buf, uint8_t len);`

- [ ] **Step 1: 新建 radio_rx 头文件**

Create `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/radio_rx.h`:

```c
#ifndef __RADIO_RX_H
#define __RADIO_RX_H

#include <stdint.h>

#define RADIO_RX_PACKET_SIZE    32

uint8_t radio_rx_init(void);
uint8_t radio_rx_poll_packet(uint8_t *buf, uint8_t len);

#endif
```

- [ ] **Step 2: 新建 radio_rx 源码文件**

Create `d:/_Project/GD32_Control/Project/Hardware/cmt2219b/radio_rx.c`:

```c
#include "radio_rx.h"
#include "cmt2219b.h"

uint8_t radio_rx_init(void)
{
    if (cmt2219b_init() != CMT2219B_OK) {
        return CMT2219B_ERROR;
    }

    if (cmt2219b_is_exist() != CMT2219B_OK) {
        return CMT2219B_ERROR;
    }

    cmt2219b_go_stby();
    cmt2219b_enable_read_fifo();
    cmt2219b_clear_interrupt_flags();
    cmt2219b_clear_rx_fifo();

    if (cmt2219b_go_rx() != CMT2219B_OK) {
        return CMT2219B_ERROR;
    }

    return CMT2219B_OK;
}

uint8_t radio_rx_poll_packet(uint8_t *buf, uint8_t len)
{
    if ((buf == 0) || (len == 0) || (len > CMT2219B_MAX_FIFO_SIZE)) {
        return CMT2219B_ERROR;
    }

    if (!cmt2219b_packet_received()) {
        return 0;
    }

    cmt2219b_go_stby();
    cmt2219b_read_fifo(buf, len);
    cmt2219b_clear_rx_fifo();
    cmt2219b_clear_interrupt_flags();
    cmt2219b_go_rx();

    return CMT2219B_OK;
}
```

- [ ] **Step 3: 接口使用约定检查**

确认上层只调用：

```c
radio_rx_init();
radio_rx_poll_packet(rx_buf, RADIO_RX_PACKET_SIZE);
```

不要在 `main.c` 直接操作 `cmt2219b_read_reg()`，除非是在调试芯片寄存器。

- [ ] **Step 4: 授权后提交本任务**

如果用户明确授权执行提交：

```bash
git add Hardware/cmt2219b/radio_rx.h Hardware/cmt2219b/radio_rx.c
git commit -m "feat: add radio rx wrapper"
```

如果用户没有授权提交，跳过本步骤。

---

## Task 6: 加入 Keil 工程

**Files:**
- Modify: `d:/_Project/GD32_Control/Project/Project/Project.uvprojx`

**Interfaces:**
- Consumes: Task 1-5 新增文件。
- Produces: Keil 工程能编译 `Hardware/cmt2219b` 目录下的 `.c` 文件，并能包含 `radio_rx.h`。

- [ ] **Step 1: 修改 IncludePath**

Find this existing line in `d:/_Project/GD32_Control/Project/Project/Project.uvprojx`:

```xml
<IncludePath>..\Firmware\GD32E23x_standard_peripheral\Include;..\Firmware\CMSIS;..\Firmware\CMSIS\GD\GD32E23x\Include;..\User;..\Hardware;..\Hardware\usart;..\Hardware\cmt2119b</IncludePath>
```

Replace with:

```xml
<IncludePath>..\Firmware\GD32E23x_standard_peripheral\Include;..\Firmware\CMSIS;..\Firmware\CMSIS\GD\GD32E23x\Include;..\User;..\Hardware;..\Hardware\usart;..\Hardware\cmt2119b;..\Hardware\cmt2219b</IncludePath>
```

- [ ] **Step 2: 在 Hardware Group 里加入 CMT2219B 文件**

In `d:/_Project/GD32_Control/Project/Project/Project.uvprojx`, inside:

```xml
<Group>
  <GroupName>Hardware</GroupName>
  <Files>
```

Add these entries after the existing `radio_tx.h` entry:

```xml
            <File>
              <FileName>cmt2219b.c</FileName>
              <FileType>1</FileType>
              <FilePath>..\Hardware\cmt2219b\cmt2219b.c</FilePath>
            </File>
            <File>
              <FileName>cmt2219b.h</FileName>
              <FileType>5</FileType>
              <FilePath>..\Hardware\cmt2219b\cmt2219b.h</FilePath>
            </File>
            <File>
              <FileName>cmt2219b_params.h</FileName>
              <FileType>5</FileType>
              <FilePath>..\Hardware\cmt2219b\cmt2219b_params.h</FilePath>
            </File>
            <File>
              <FileName>cmt2219b_port.c</FileName>
              <FileType>1</FileType>
              <FilePath>..\Hardware\cmt2219b\cmt2219b_port.c</FilePath>
            </File>
            <File>
              <FileName>cmt2219b_port.h</FileName>
              <FileType>5</FileType>
              <FilePath>..\Hardware\cmt2219b\cmt2219b_port.h</FilePath>
            </File>
            <File>
              <FileName>cmt2219b_spi.c</FileName>
              <FileType>1</FileType>
              <FilePath>..\Hardware\cmt2219b\cmt2219b_spi.c</FilePath>
            </File>
            <File>
              <FileName>cmt2219b_spi.h</FileName>
              <FileType>5</FileType>
              <FilePath>..\Hardware\cmt2219b\cmt2219b_spi.h</FilePath>
            </File>
            <File>
              <FileName>radio_rx.c</FileName>
              <FileType>1</FileType>
              <FilePath>..\Hardware\cmt2219b\radio_rx.c</FilePath>
            </File>
            <File>
              <FileName>radio_rx.h</FileName>
              <FileType>5</FileType>
              <FilePath>..\Hardware\cmt2219b\radio_rx.h</FilePath>
            </File>
```

- [ ] **Step 3: 编译验证工程文件配置**

在 Keil uVision 中打开：

```text
d:/_Project/GD32_Control/Project/Project/Project.uvprojx
```

执行：

```text
Project -> Rebuild all target files
```

Expected:

```text
0 Error(s)
```

如果出现 `cannot open source input file "radio_rx.h"`，说明 IncludePath 没加对；重新检查 Step 1。

如果出现 `source file not found ..\Hardware\cmt2219b\xxx.c`，说明 Step 2 的路径拼写不对。

- [ ] **Step 4: 授权后提交本任务**

如果用户明确授权执行提交：

```bash
git add Project/Project.uvprojx
git commit -m "build: add cmt2219b files to keil project"
```

如果用户没有授权提交，跳过本步骤。

---

## Task 7: 修改 main.c 做首版接收验证

**Files:**
- Modify: `d:/_Project/GD32_Control/Project/User/main.c`

**Interfaces:**
- Consumes: Task 5 的 `radio_rx_init()` 和 `radio_rx_poll_packet()`。
- Produces: 主循环同时初始化发射和接收，收到数据后增加 `rx_count`，方便断点观察 `rx_buf`。

- [ ] **Step 1: 替换 main.c 内容**

Replace `d:/_Project/GD32_Control/Project/User/main.c` with:

```c
#include "gd32e23x.h"
#include "systick.h"
#include "radio_tx.h"
#include "radio_rx.h"
#include <stdint.h>

static uint8_t tx_buf[32];
static uint8_t rx_buf[32];
static volatile uint32_t rx_count;
static volatile uint8_t radio_rx_ready;

static void test_packet_init(void)
{
    uint8_t i;

    for (i = 0; i < sizeof(tx_buf); i++) {
        tx_buf[i] = i + 1;
    }
}

int main(void)
{
    systick_config();

    test_packet_init();

    radio_tx_init();
    radio_rx_ready = radio_rx_init();

    while (1) {
        if (radio_rx_ready) {
            if (radio_rx_poll_packet(rx_buf, sizeof(rx_buf))) {
                rx_count++;
            }
        }

        radio_tx_send(tx_buf, sizeof(tx_buf));
        delay_ms(1000);
    }
}
```

- [ ] **Step 2: 编译验证 main.c**

在 Keil uVision 中执行：

```text
Project -> Rebuild all target files
```

Expected:

```text
0 Error(s)
```

如果出现 `radio_rx.h` 找不到，返回 Task 6 检查 IncludePath。

- [ ] **Step 3: 下载并设置调试观察变量**

下载程序后，在 Keil Debug 里观察：

```text
radio_rx_ready
rx_count
rx_buf
```

Expected:

```text
radio_rx_ready = 1
```

如果 `radio_rx_ready = 0`，优先检查 PB13/PB14/PB15 的 SPI 读写波形，以及 CMT2219B 供电和 CSB/FCSB 连接。

- [ ] **Step 4: 授权后提交本任务**

如果用户明确授权执行提交：

```bash
git add User/main.c
git commit -m "test: poll cmt2219b rx in main loop"
```

如果用户没有授权提交，跳过本步骤。

---

## Task 8: 硬件验证顺序

**Files:**
- No code files.

**Interfaces:**
- Consumes: Task 1-7 完成后的固件。
- Produces: 确认接收端移植是否达到首版可用状态。

- [ ] **Step 1: 验证供电和静态电平**

上电后，用万用表或示波器确认：

```text
PH2219BBA VCC = 3.3V
PH2219BBA GND = GND
PB13 / CSB    = 高电平空闲
PB12 / FCSB   = 高电平空闲
PB14 / CLK    = 低电平空闲
PB15 / SDIO   = 高电平或输入上拉状态
```

- [ ] **Step 2: 验证初始化 SPI 波形**

在 `radio_rx_init()` 里打断点单步，观察：

```text
PB13 / CSB  有寄存器读写片选波形
PB14 / CLK  有连续 8bit 时钟
PB15 / SDIO 写寄存器阶段有输出数据
PB15 / SDIO 读寄存器阶段切换为输入
```

- [ ] **Step 3: 验证芯片存在检测**

在 `cmt2219b_is_exist()` 里观察：

```text
back = cmt2219b_read_reg(0x48)
dat  = cmt2219b_read_reg(0x48)
```

Expected:

```text
dat = 0xAA
radio_rx_ready = 1
```

如果 `dat != 0xAA`：

```text
1. 检查 PB13 是否真的接到 CSB
2. 检查 PB14 是否真的接到 CLK
3. 检查 PB15 是否真的接到 SDIO
4. 检查 PB12 是否保持高，不要误拉低 FIFO 片选
5. 检查 SDIO 读寄存器时是否已经切为输入
```

- [ ] **Step 4: 验证进入 RX 状态**

在 `radio_rx_init()` 末尾观察：

```text
cmt2219b_go_rx() 返回 CMT2219B_OK
```

Expected:

```text
radio_rx_ready = 1
```

如果 `cmt2219b_go_rx()` 失败，先临时在 `cmt2219b_auto_switch_status()` 里观察 `cmt2219b_get_chip_status()` 返回值。

- [ ] **Step 5: 验证收到包**

保持发送端 `radio_tx_send(tx_buf, 32)` 每秒执行一次。调试观察：

```text
PB10 / GPIO3
rx_count
rx_buf[0..31]
```

Expected:

```text
PB10 在收到包时出现有效电平
rx_count 递增
rx_buf 能读到 FIFO 数据
```

如果 `PB10` 没变化：

```text
1. 确认 CMT2219B GPIO3 真的接 PB10
2. 确认 `CMT2219B_GPIO3_SEL_INT2` 配置为 0x20
3. 确认 `CMT2219B_INT_SEL_PKT_DONE` 配置为 0x19
4. 确认接收参数与发送参数匹配
5. 拉开发射天线和接收天线距离，避免同板自发自收过载
```

如果 `PB10` 有变化但 `rx_buf` 全 0 或乱值：

```text
1. 检查 `cmt2219b_enable_read_fifo()` 是否在进入 RX 前调用
2. 检查 FIFO 读时 PB12/FCSB 是否按字节拉低
3. 检查 FIFO 读时 PB15/SDIO 是否保持输入
4. 检查 `radio_rx_poll_packet()` 是否读完 FIFO 后重新进入 RX
```

---

## Self-Review

### Spec coverage

- 新增 `Hardware/cmt2219b/`：Task 1-5 覆盖。
- 接收端引脚 PB12/PB13/PB14/PB15/PB10：Task 1 覆盖。
- 官方 Demo 参数：Task 3 覆盖。
- 3 线 SPI 和 SDIO 输入/输出切换：Task 2 覆盖。
- 芯片初始化、FIFO、RX、中断：Task 4 覆盖。
- 应用层 `radio_rx`：Task 5 覆盖。
- Keil 工程集成：Task 6 覆盖。
- 主循环验证：Task 7 覆盖。
- 硬件验证顺序：Task 8 覆盖。
- 发射端保持不变：Global Constraints 覆盖，所有任务都没有修改 `Hardware/cmt2119b/`。

### Placeholder scan

No `TBD`, no incomplete file names, no undefined task-level interfaces.

### Type consistency

- 所有公开接口使用 `uint8_t`、`uint16_t`、`uint32_t`，与现有 `cmt2119b` 风格一致。
- `radio_rx_poll_packet()` 调用的 `cmt2219b_*` 函数都在 Task 4 定义。
- Keil IncludePath 包含 `..\Hardware\cmt2219b`，能找到 `radio_rx.h`。
